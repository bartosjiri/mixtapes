# ------------------------------------------------------------------------
# PLAYLISTS CONFIG (optional)
# ------------------------------------------------------------------------
# id - Spotify playlist ID (string)
# image - custom playlist cover image (string; optional)
# color - background color in any CSS format (string; optional)
# enabled - include playlist on site (boolean; optional)
# order - order priority in the list (number; optional)
# ------------------------------------------------------------------------

- id:
  image:
  color:
  enabled:
  order: 
